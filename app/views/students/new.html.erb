<div class="d-flex align-items-center">
	<h1>学生の登録</h1>
	<div class="ml-auto">
		<%= link_to '学生一覧', students_path, class: 'btn btn-outline-dark' %>
	</div>
</div>

<%= render partial: 'form', locals: {student: @student} %>